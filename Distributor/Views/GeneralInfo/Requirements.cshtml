@using Distributor.Extenstions;

@model IEnumerable<Distributor.ViewModels.RequirementListingGeneralInfoView>

@{ 
    var listingId = Guid.Empty;
    var offerButton = "";
    var saveButton = "";
    var offerQuantityField = "";
    var offerQuantity = 0.00M;
    var currentAppUserId = User.Identity.GetAppUserId().ToString();
    var appUserId = "";
}

@section Scripts {
    @*@Scripts.Render("~/bundles/jqueryval")*@
    @Scripts.Render("~/css/vendor")
    @*<script type="text/javascript" src="http://cdn.datatables.net/1.10.16/js/jquery.dataTables.min.js"></script>*@

    <script type="text/javascript">
        
        $(function () {
            //show/hide offer details to edit
            $('.showhideofferdetails').click(function () {
                var id = $(this).closest('tr').attr('id');
                var offerButtonId = "offerbutton" + id.toString();
                var saveButtonId = "savebutton" + id.toString();
                var offerQuantityFieldId = "offerquantityfield" + id.toString();
                if ($("#" + offerButtonId).prop("disabled") == false) {
                    $("#" + offerButtonId).prop("disabled", true);
                    $("#" + saveButtonId).prop("disabled", false);
                    $("#" + offerQuantityFieldId).prop("disabled", false);
                } else if ($("#" + saveButtonId).prop("disabled") == false && $("#" + offerQuantityFieldId).val() > 0) {
                    var offerQty = $("#" + offerQuantityFieldId).val();
                    if (window.confirm("Submit this offer?")) {
                        var url = "/RequirementListings/SubmitRequirementOffer";

                        $.ajax({
                            url: url,
                            data: { listingId: id, offerQuantity: offerQty },
                            cache: false,
                            type: "POST",
                            success: function (result) {
                                location.reload(true);
                            }
                        });
                    }
                }
            });
        });
    </script>
}

<nav class="navbar navbar-fixed-top">
    <div class="navbar-inverse">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                @Html.ActionLink("Distributor", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
            </div>
            <div class="navbar-collapse collapse">
                @Html.Partial("_LoginPartial")
            </div>
        </div>
    </div>
    <nav class="navbar navbar-default">
        <div class="container">
            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                @*<a class="navbar-brand" href="#">Brand</a>*@
            </div>

            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav">
                    <li class="disabled">@Html.ActionLink("GENERAL INFO", "General", "Home")</li>
                    <li class="active">@Html.ActionLink("Requests", "Requirements", "GeneralInfo")</li>
                    <li>@Html.ActionLink("Available", "Available", "GeneralInfo")</li>
                    <li>@Html.ActionLink("Campaigns", "Campaigns", "GeneralInfo")</li>
                </ul>
            </div><!-- /.navbar-collapse -->
        </div><!-- /.container -->
    </nav>
</nav>

<div class="row" style="padding-top:55px">
    <div class="col-lg-12">
        <h1 class="page-header" style="margin: 10px 0px 10px">General Info <small>Requests</small></h1>
        <ol class="breadcrumb">
            <li>@Html.ActionLink("Home", "Index", "Home")</li>
            <li class="active">Requests</li>
        </ol>
        <p class="lead">Current requests.</p>
    </div>
</div>

<div class="table-responsive">
    @if (Model.Count() == 0)
    {
        <p>No details to show</p>
    }
    else
    {
        <table id="requestsTable" class="table table-hover">
            <thead>
                <tr class="info">
                    <th colspan="3">Actions</th>
                    <th>Description</th>
                    <th>Type</th>
                    <th>Qty reqd</th>
                    <th>UoM</th>
                    <th>From</th>
                    <th>To</th>
                    <th style="text-align:center">Damaged</th>
                    <th style="text-align:center">Out of date</th>
                    <th style="text-align:center">Collection</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    listingId = item.RequirementListing.ListingId;
                    offerButton = "offerbutton" + listingId.ToString();
                    saveButton = "savebutton" + listingId.ToString();
                    offerQuantityField = "offerquantityfield" + listingId.ToString();
                    offerQuantity = item.OfferQuantity;
                    appUserId = item.RequirementListing.ListingOriginatorAppUserId.ToString();

                    if (item.CompanyLevelBlock && item.DisplayBlocks)
                    {
                        <tr id=@listingId style="color:darkred">
                            @if (offerQuantity == 0)
                            {
                                if (item.AllowBranchTrading)
                                {
                                    if (appUserId != currentAppUserId)
                                    {
                                        <td><button type="submit" class="btn btn-xs btn-primary btn-block showhideofferdetails" id=@offerButton><span class="glyphicon glyphicon-usd"></span></button></td>
                                    }
                                    else
                                    {
                                        <td><button type="submit" class="btn btn-xs btn-primary btn-block showhideofferdetails" id=@offerButton disabled><span class="glyphicon glyphicon-usd"></span></button></td>
                                    }
                                }
                                else
                                {
                                    if (item.RequirementListing.ListingOriginatorCompanyId != ViewBag.CurrentCompanyId)
                                    {
                                        <td><button type="submit" class="btn btn-xs btn-primary btn-block showhideofferdetails" id=@offerButton><span class="glyphicon glyphicon-usd"></span></button></td>
                                    }
                                    else
                                    {
                                        <td><button type="submit" class="btn btn-xs btn-primary btn-block showhideofferdetails" id=@offerButton disabled><span class="glyphicon glyphicon-usd"></span></button></td>
                                    }
                                }
                            }
                            else
                            {
                                <td><button type="submit" class="btn btn-xs btn-primary btn-block showhideofferdetails" id=@offerButton disabled><span class="glyphicon glyphicon-usd"></span></button></td>
                            }
                            <td>@Html.TextBoxFor(modelItem => item.OfferQuantity, new { id = @offerQuantityField, disabled = "disabled", style = "width:70px" })</td>
                            <td><button type="submit" class="btn btn-xs btn-success btn-block showhideofferdetails" id=@saveButton disabled><span class="glyphicon glyphicon-ok"></span></button></td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'">@Html.DisplayFor(modelItem => item.RequirementListing.ItemDescription)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'">@Html.DisplayFor(modelItem => item.RequirementListing.ItemType)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'">@Html.DisplayFor(modelItem => item.RequirementListing.QuantityOutstanding)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'">@Html.DisplayFor(modelItem => item.RequirementListing.UoM)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'">@Html.HiddenFor(modelItem => item.RequirementListing.RequiredFrom)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'">@Html.HiddenFor(modelItem => item.RequirementListing.RequiredTo)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'" style="text-align:center">@Html.DisplayFor(modelItem => item.RequirementListing.AcceptDamagedItems)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'" style="text-align:center">@Html.DisplayFor(modelItem => item.RequirementListing.AcceptOutOfDateItems)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'" style="text-align:center">@Html.DisplayFor(modelItem => item.RequirementListing.CollectionAvailable)</td>
                        </tr>
                    }
                    else if (item.BranchLevelBlock && item.DisplayBlocks)
                    {
                        <tr id=@listingId style="color:indianred">
                            @if (offerQuantity == 0)
                            {
                                if (item.AllowBranchTrading)
                                {
                                    if (appUserId != currentAppUserId)
                                    {
                                        <td><button type="submit" class="btn btn-xs btn-primary btn-block showhideofferdetails" id=@offerButton><span class="glyphicon glyphicon-usd"></span></button></td>
                                    }
                                    else
                                    {
                                        <td><button type="submit" class="btn btn-xs btn-primary btn-block showhideofferdetails" id=@offerButton disabled><span class="glyphicon glyphicon-usd"></span></button></td>
                                    }
                                }
                                else
                                {
                                    if (item.RequirementListing.ListingOriginatorCompanyId != ViewBag.CurrentCompanyId)
                                    {
                                        <td><button type="submit" class="btn btn-xs btn-primary btn-block showhideofferdetails" id=@offerButton><span class="glyphicon glyphicon-usd"></span></button></td>
                                    }
                                    else
                                    {
                                        <td><button type="submit" class="btn btn-xs btn-primary btn-block showhideofferdetails" id=@offerButton disabled><span class="glyphicon glyphicon-usd"></span></button></td>
                                    }
                                }
                            }
                            else
                            {
                                <td><button type="submit" class="btn btn-xs btn-primary btn-block showhideofferdetails" id=@offerButton disabled><span class="glyphicon glyphicon-usd"></span></button></td>
                            }
                            <td>@Html.TextBoxFor(modelItem => item.OfferQuantity, new { id = @offerQuantityField, disabled = "disabled", style = "width:70px" })</td>
                            <td><button type="submit" class="btn btn-xs btn-success btn-block showhideofferdetails" id=@saveButton disabled><span class="glyphicon glyphicon-ok"></span></button></td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'">@Html.DisplayFor(modelItem => item.RequirementListing.ItemDescription)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'">@Html.DisplayFor(modelItem => item.RequirementListing.ItemType)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'">@Html.DisplayFor(modelItem => item.RequirementListing.QuantityOutstanding)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'">@Html.DisplayFor(modelItem => item.RequirementListing.UoM)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'">@Html.HiddenFor(modelItem => item.RequirementListing.RequiredFrom)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'">@Html.HiddenFor(modelItem => item.RequirementListing.RequiredTo)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'" style="text-align:center">@Html.DisplayFor(modelItem => item.RequirementListing.AcceptDamagedItems)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'" style="text-align:center">@Html.DisplayFor(modelItem => item.RequirementListing.AcceptOutOfDateItems)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'" style="text-align:center">@Html.DisplayFor(modelItem => item.RequirementListing.CollectionAvailable)</td>
                        </tr>
                    }
                    else if (item.UserLevelBlock && item.DisplayBlocks)
                    {
                        <tr id=@listingId style="color:red">
                            @if (offerQuantity == 0)
                            {
                                if (item.AllowBranchTrading)
                                {
                                    if (appUserId != currentAppUserId)
                                    {
                                        <td><button type="submit" class="btn btn-xs btn-primary btn-block showhideofferdetails" id=@offerButton><span class="glyphicon glyphicon-usd"></span></button></td>
                                    }
                                    else
                                    {
                                        <td><button type="submit" class="btn btn-xs btn-primary btn-block showhideofferdetails" id=@offerButton disabled><span class="glyphicon glyphicon-usd"></span></button></td>
                                    }
                                }
                                else
                                {
                                    if (item.RequirementListing.ListingOriginatorCompanyId != ViewBag.CurrentCompanyId)
                                    {
                                        <td><button type="submit" class="btn btn-xs btn-primary btn-block showhideofferdetails" id=@offerButton><span class="glyphicon glyphicon-usd"></span></button></td>
                                    }
                                    else
                                    {
                                        <td><button type="submit" class="btn btn-xs btn-primary btn-block showhideofferdetails" id=@offerButton disabled><span class="glyphicon glyphicon-usd"></span></button></td>
                                    }
                                }
                            }
                            else
                            {
                                <td><button type="submit" class="btn btn-xs btn-primary btn-block showhideofferdetails" id=@offerButton disabled><span class="glyphicon glyphicon-usd"></span></button></td>
                            }
                            <td>@Html.TextBoxFor(modelItem => item.OfferQuantity, new { id = @offerQuantityField, disabled = "disabled", style = "width:70px" })</td>
                            <td><button type="submit" class="btn btn-xs btn-success btn-block showhideofferdetails" id=@saveButton disabled><span class="glyphicon glyphicon-ok"></span></button></td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'">@Html.DisplayFor(modelItem => item.RequirementListing.ItemDescription)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'">@Html.DisplayFor(modelItem => item.RequirementListing.ItemType)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'">@Html.DisplayFor(modelItem => item.RequirementListing.QuantityOutstanding)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'">@Html.DisplayFor(modelItem => item.RequirementListing.UoM)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'">@Html.HiddenFor(modelItem => item.RequirementListing.RequiredFrom)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'">@Html.HiddenFor(modelItem => item.RequirementListing.RequiredTo)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'" style="text-align:center">@Html.DisplayFor(modelItem => item.RequirementListing.AcceptDamagedItems)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'" style="text-align:center">@Html.DisplayFor(modelItem => item.RequirementListing.AcceptOutOfDateItems)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'" style="text-align:center">@Html.DisplayFor(modelItem => item.RequirementListing.CollectionAvailable)</td>
                        </tr>
                    }
                    else if ((item.CompanyLevelBlock || item.BranchLevelBlock || item.UserLevelBlock) && !item.DisplayBlocks)
                    {
                        //do nothing, don't show the row
                    }
                    else if (item.UserLevelOwner && item.DisplayMyRecords)
                    {
                        <tr id=@listingId style="color:green">
                            @if (offerQuantity == 0)
                            {
                                if (item.AllowBranchTrading)
                                {
                                    if (appUserId != currentAppUserId)
                                    {
                                        <td><button type="submit" class="btn btn-xs btn-primary btn-block showhideofferdetails" id=@offerButton><span class="glyphicon glyphicon-usd"></span></button></td>
                                    }
                                    else
                                    {
                                        <td><button type="submit" class="btn btn-xs btn-primary btn-block showhideofferdetails" id=@offerButton disabled><span class="glyphicon glyphicon-usd"></span></button></td>
                                    }
                                }
                                else
                                {
                                    if (item.RequirementListing.ListingOriginatorCompanyId != ViewBag.CurrentCompanyId)
                                    {
                                        <td><button type="submit" class="btn btn-xs btn-primary btn-block showhideofferdetails" id=@offerButton><span class="glyphicon glyphicon-usd"></span></button></td>
                                    }
                                    else
                                    {
                                        <td><button type="submit" class="btn btn-xs btn-primary btn-block showhideofferdetails" id=@offerButton disabled><span class="glyphicon glyphicon-usd"></span></button></td>
                                    }
                                }
                            }
                            else
                            {
                                <td><button type="submit" class="btn btn-xs btn-primary btn-block showhideofferdetails" id=@offerButton disabled><span class="glyphicon glyphicon-usd"></span></button></td>
                            }
                            <td>@Html.TextBoxFor(modelItem => item.OfferQuantity, new { id = @offerQuantityField, disabled = "disabled", style = "width:70px" })</td>
                            <td><button type="submit" class="btn btn-xs btn-success btn-block showhideofferdetails" id=@saveButton disabled><span class="glyphicon glyphicon-ok"></span></button></td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'">@Html.DisplayFor(modelItem => item.RequirementListing.ItemDescription)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'">@Html.DisplayFor(modelItem => item.RequirementListing.ItemType)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'">@Html.DisplayFor(modelItem => item.RequirementListing.QuantityOutstanding)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'">@Html.DisplayFor(modelItem => item.RequirementListing.UoM)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'">@Html.HiddenFor(modelItem => item.RequirementListing.RequiredFrom)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'">@Html.HiddenFor(modelItem => item.RequirementListing.RequiredTo)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'" style="text-align:center">@Html.DisplayFor(modelItem => item.RequirementListing.AcceptDamagedItems)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'" style="text-align:center">@Html.DisplayFor(modelItem => item.RequirementListing.AcceptOutOfDateItems)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'" style="text-align:center">@Html.DisplayFor(modelItem => item.RequirementListing.CollectionAvailable)</td>
                        </tr>
                    }
                    else if (item.UserLevelOwner && !item.DisplayMyRecords)
                    {
                        //do nothing, don't show the row
                    }
                    else if (item.BranchLevelOwner && item.DisplayMyBranchRecords)
                    {
                        <tr id=@listingId style="color:forestgreen">
                            @if (offerQuantity == 0)
                            {
                                if (item.AllowBranchTrading)
                                {
                                    if (appUserId != currentAppUserId)
                                    {
                                        <td><button type="submit" class="btn btn-xs btn-primary btn-block showhideofferdetails" id=@offerButton><span class="glyphicon glyphicon-usd"></span></button></td>
                                    }
                                    else
                                    {
                                        <td><button type="submit" class="btn btn-xs btn-primary btn-block showhideofferdetails" id=@offerButton disabled><span class="glyphicon glyphicon-usd"></span></button></td>
                                    }
                                }
                                else
                                {
                                    if (item.RequirementListing.ListingOriginatorCompanyId != ViewBag.CurrentCompanyId)
                                    {
                                        <td><button type="submit" class="btn btn-xs btn-primary btn-block showhideofferdetails" id=@offerButton><span class="glyphicon glyphicon-usd"></span></button></td>
                                    }
                                    else
                                    {
                                        <td><button type="submit" class="btn btn-xs btn-primary btn-block showhideofferdetails" id=@offerButton disabled><span class="glyphicon glyphicon-usd"></span></button></td>
                                    }
                                }
                            }
                            else
                            {
                                <td><button type="submit" class="btn btn-xs btn-primary btn-block showhideofferdetails" id=@offerButton disabled><span class="glyphicon glyphicon-usd"></span></button></td>
                            }
                            <td>@Html.TextBoxFor(modelItem => item.OfferQuantity, new { id = @offerQuantityField, disabled = "disabled", style = "width:70px" })</td>
                            <td><button type="submit" class="btn btn-xs btn-success btn-block showhideofferdetails" id=@saveButton disabled><span class="glyphicon glyphicon-ok"></span></button></td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'">@Html.DisplayFor(modelItem => item.RequirementListing.ItemDescription)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'">@Html.DisplayFor(modelItem => item.RequirementListing.ItemType)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'">@Html.DisplayFor(modelItem => item.RequirementListing.QuantityOutstanding)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'">@Html.DisplayFor(modelItem => item.RequirementListing.UoM)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'">@Html.HiddenFor(modelItem => item.RequirementListing.RequiredFrom)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'">@Html.HiddenFor(modelItem => item.RequirementListing.RequiredTo)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'" style="text-align:center">@Html.DisplayFor(modelItem => item.RequirementListing.AcceptDamagedItems)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'" style="text-align:center">@Html.DisplayFor(modelItem => item.RequirementListing.AcceptOutOfDateItems)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'" style="text-align:center">@Html.DisplayFor(modelItem => item.RequirementListing.CollectionAvailable)</td>
                        </tr>
                    }
                    else if (item.BranchLevelOwner && !item.DisplayMyBranchRecords)
                    {
                        //do nothing, don't show the row
                    }
                    else if (item.CompanyLevelOwner && item.DisplayMyCompanyRecords)
                    {
                        <tr id=@listingId style="color:darkgreen">
                            @if (offerQuantity == 0)
                            {
                                if (item.AllowBranchTrading)
                                {
                                    if (appUserId != currentAppUserId)
                                    {
                                        <td><button type="submit" class="btn btn-xs btn-primary btn-block showhideofferdetails" id=@offerButton><span class="glyphicon glyphicon-usd"></span></button></td>
                                    }
                                    else
                                    {
                                        <td><button type="submit" class="btn btn-xs btn-primary btn-block showhideofferdetails" id=@offerButton disabled><span class="glyphicon glyphicon-usd"></span></button></td>
                                    }
                                }
                                else
                                {
                                    if (item.RequirementListing.ListingOriginatorCompanyId != ViewBag.CurrentCompanyId)
                                    {
                                        <td><button type="submit" class="btn btn-xs btn-primary btn-block showhideofferdetails" id=@offerButton><span class="glyphicon glyphicon-usd"></span></button></td>
                                    }
                                    else
                                    {
                                        <td><button type="submit" class="btn btn-xs btn-primary btn-block showhideofferdetails" id=@offerButton disabled><span class="glyphicon glyphicon-usd"></span></button></td>
                                    }
                                }
                            }
                            else
                            {
                                <td><button type="submit" class="btn btn-xs btn-primary btn-block showhideofferdetails" id=@offerButton disabled><span class="glyphicon glyphicon-usd"></span></button></td>
                            }
                            <td>@Html.TextBoxFor(modelItem => item.OfferQuantity, new { id = @offerQuantityField, disabled = "disabled", style = "width:70px" })</td>
                            <td><button type="submit" class="btn btn-xs btn-success btn-block showhideofferdetails" id=@saveButton disabled><span class="glyphicon glyphicon-ok"></span></button></td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'">@Html.DisplayFor(modelItem => item.RequirementListing.ItemDescription)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'">@Html.DisplayFor(modelItem => item.RequirementListing.ItemType)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'">@Html.DisplayFor(modelItem => item.RequirementListing.QuantityOutstanding)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'">@Html.DisplayFor(modelItem => item.RequirementListing.UoM)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'">@Html.HiddenFor(modelItem => item.RequirementListing.RequiredFrom)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'">@Html.HiddenFor(modelItem => item.RequirementListing.RequiredTo)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'" style="text-align:center">@Html.DisplayFor(modelItem => item.RequirementListing.AcceptDamagedItems)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'" style="text-align:center">@Html.DisplayFor(modelItem => item.RequirementListing.AcceptOutOfDateItems)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'" style="text-align:center">@Html.DisplayFor(modelItem => item.RequirementListing.CollectionAvailable)</td>
                        </tr>
                    }
                    else if (item.CompanyLevelOwner && !item.DisplayMyCompanyRecords)
                    {
                        //do nothing, don't show the row
                    }
                    else 
                    {
                        <tr id=@listingId>
                            @if (offerQuantity == 0)
                            {
                                if (item.AllowBranchTrading)
                                {
                                    if (appUserId != currentAppUserId)
                                    {
                                        <td><button type="submit" class="btn btn-xs btn-primary btn-block showhideofferdetails" id=@offerButton><span class="glyphicon glyphicon-usd"></span></button></td>
                                    }
                                    else
                                    {
                                        <td><button type="submit" class="btn btn-xs btn-primary btn-block showhideofferdetails" id=@offerButton disabled><span class="glyphicon glyphicon-usd"></span></button></td>
                                    }
                                }
                                else
                                {
                                    if (item.RequirementListing.ListingOriginatorCompanyId != ViewBag.CurrentCompanyId)
                                    {
                                        <td><button type="submit" class="btn btn-xs btn-primary btn-block showhideofferdetails" id=@offerButton><span class="glyphicon glyphicon-usd"></span></button></td>
                                    }
                                    else
                                    {
                                        <td><button type="submit" class="btn btn-xs btn-primary btn-block showhideofferdetails" id=@offerButton disabled><span class="glyphicon glyphicon-usd"></span></button></td>
                                    }
                                }
                            }
                            else
                            {
                                <td><button type="submit" class="btn btn-xs btn-primary btn-block showhideofferdetails" id=@offerButton disabled><span class="glyphicon glyphicon-usd"></span></button></td>
                            }
                            <td>@Html.TextBoxFor(modelItem => item.OfferQuantity, new { id = @offerQuantityField, disabled = "disabled", style = "width:70px" })</td>
                            <td><button type="submit" class="btn btn-xs btn-success btn-block showhideofferdetails" id=@saveButton disabled><span class="glyphicon glyphicon-ok"></span></button></td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'">@Html.DisplayFor(modelItem => item.RequirementListing.ItemDescription)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'">@Html.DisplayFor(modelItem => item.RequirementListing.ItemType)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'">@Html.DisplayFor(modelItem => item.RequirementListing.QuantityOutstanding)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'">@Html.DisplayFor(modelItem => item.RequirementListing.UoM)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'">@Html.HiddenFor(modelItem => item.RequirementListing.RequiredFrom)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'">@Html.HiddenFor(modelItem => item.RequirementListing.RequiredTo)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'" style="text-align:center">@Html.DisplayFor(modelItem => item.RequirementListing.AcceptDamagedItems)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'" style="text-align:center">@Html.DisplayFor(modelItem => item.RequirementListing.AcceptOutOfDateItems)</td>
                            <td onclick="location.href='@Url.Action("Details", "RequirementListings", new { id = item.RequirementListing.ListingId })'" style="text-align:center">@Html.DisplayFor(modelItem => item.RequirementListing.CollectionAvailable)</td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    }
</div>

<div class="form-group">
        @Html.ActionLink("Create listing", "Create", "RequirementListings", "", new { @class = "btn btn-primary" })
        @Html.ActionLink("Offers", "Offers", "ManageListings", "", new { @class = "btn btn-default" })
        @Html.ActionLink("Orders", "Orders", "ManageListings", "", new { @class = "btn btn-default" })
</div>